<resources>

    <!-- Base application theme. -->
    <!--Para desactivar la barra hay que usar NoActionBar-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="LoginTheme" parent="AppTheme">
        <item name="android:textColor">@color/colorPrimaryLogin</item>
    </style>
    <style name="Widget.Button.Transparent" parent="@android:style/Widget.Button">
        <!--Con @null anulamos la propiedad-->
        <item name="android:background">@null</item>
        <item name="android:textAppearance">?android:attr/textAppearanceSmall</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:layout_marginTop">16dp</item>
    </style>
    <style name="LoginEditText">
        <item name="android:padding">8dp</item>
    </style>
    <style name="LoginCheckBox" parent="android:Widget.CompoundButton.CheckBox">
        <item name="colorAccent">@android:color/darker_gray</item>
    </style>
    <style name="LetrasBlancasCheckBox" parent="android:Widget.CompoundButton.CheckBox">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAppearance">?android:attr/textAppearanceSmall</item>
    </style>

</resources>
